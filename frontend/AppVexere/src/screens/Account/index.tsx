import React from 'react';
import { Text } from 'react-native';
import withSafeView from '../../hocs/withSafeView';

const AccountScreen = () => {
    return (
        <Text>AccountScreen</Text>
    )
}

export default withSafeView(AccountScreen);